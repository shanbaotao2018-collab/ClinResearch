# 文献综述智能体字段说明与真实运行示例

更新日期：2026-07-02

## 1. 这份文档是干什么的

这份文档面向科研新手，用最直白的方式说明：

1. 这套文献综述智能体目前有哪些 workflow
2. 每个 workflow 需要输入什么字段
3. 每个字段分别是什么意思
4. 系统跑完后会输出什么结果
5. 我们前面已经真实跑过的 3 个项目是什么样子

建议把这份文档理解为：

`操作说明 + 字段字典 + 真实样例`

## 2. 当前有哪两个 Workflow

### 2.1 Workflow 1：文献综述-项目初始化与检索准备

这个 workflow 负责前半段准备工作：

1. 创建一个综述项目
2. 生成一版基础检索式
3. 导入文献列表
4. 自动去重
5. 导出当前项目包

一句话理解：

`先把课题和文献池搭起来`

### 2.2 Workflow 2：文献综述-筛选决策与 PRISMA

这个 workflow 负责文献筛选和统计更新：

1. 对单篇文献做纳入或排除判断
2. 记录理由
3. 自动更新 PRISMA 计数

一句话理解：

`每筛一篇文献，系统自动记账`

## 3. Workflow 1 字段说明

## 3.1 输入字段

| 字段名 | 中文含义 | 你怎么理解 |
|---|---|---|
| `title` | 综述项目标题 | 这个课题叫什么 |
| `research_question` | 研究问题 | 你到底想回答什么临床问题 |
| `pico_population` | PICO-P | 研究对象是谁 |
| `pico_intervention` | PICO-I | 干预措施是什么 |
| `pico_comparator` | PICO-C | 和谁比较 |
| `pico_outcome` | PICO-O | 最关心什么结局 |
| `inclusion_criteria` | 纳入标准 | 什么样的文献可以留下 |
| `exclusion_criteria` | 排除标准 | 什么样的文献要去掉 |
| `citations_payload_json` | 文献导入载荷 | 要导入的文献清单，当前用 JSON 传入 |

### 3.2 输入字段白话解释

#### `title`

项目标题，类似论文题目或课题名。

示例：

`SGLT2 抑制剂与 2 型糖尿病患者心衰住院风险文献综述`

#### `research_question`

这是最核心的问题，通常是一句话。

示例：

`在 2 型糖尿病患者中，SGLT2 抑制剂是否降低心力衰竭住院风险？`

#### `pico_population`

研究对象是谁。

示例：

`成人 2 型糖尿病患者`

#### `pico_intervention`

你研究的干预措施是什么。

示例：

`SGLT2 抑制剂`

#### `pico_comparator`

用来对比的组别。

示例：

`安慰剂或标准治疗`

#### `pico_outcome`

你最关心的结果指标。

示例：

`心力衰竭住院`

#### `inclusion_criteria`

什么样的研究可以被留下。

示例：

`随机对照试验；成人；报告心衰住院或主要心血管结局。`

#### `exclusion_criteria`

什么样的研究要被剔除。

示例：

`非临床研究；重复发表；无关键结局。`

#### `citations_payload_json`

这是当前 MVP 里最“技术”的一个字段。

它本质上就是一组待导入的文献记录，通常包含：

| 子字段 | 含义 |
|---|---|
| `source` | 来源数据库，比如 `pubmed` |
| `citations` | 文献列表 |
| `title` | 文献标题 |
| `external_id` | 外部数据库编号或业务编号 |
| `abstract` | 摘要 |
| `authors` | 作者 |
| `publication_year` | 发表年份 |
| `doi` | DOI 编号 |

你可以把它理解成：

`把一批文献信息打包后丢给系统`

## 3.3 输出字段

| 字段名 | 中文含义 | 你怎么理解 |
|---|---|---|
| `project_id` | 项目编号 | 后面所有动作都靠它找到这个项目 |
| `search_query` | 检索式 | 自动生成的数据库搜索草案 |
| `imported_count` | 导入数量 | 一共导入了多少条文献 |
| `removed_count` | 去重数量 | 自动删掉了多少条重复文献 |
| `export_bundle` | 导出结果包 | 当前项目的完整结构化结果 |

### 3.4 `export_bundle` 里有什么

`export_bundle` 里主要有 4 部分：

| 子字段 | 含义 |
|---|---|
| `project` | 项目基本信息 |
| `citations` | 当前项目的文献列表 |
| `prisma` | 当前 PRISMA 统计 |
| `audit_logs` | 系统操作日志 |

#### `export_bundle.project`

这里会保存项目本身的信息：

| 子字段 | 含义 |
|---|---|
| `title` | 项目标题 |
| `research_question` | 研究问题 |
| `pico_population` | PICO-P |
| `pico_intervention` | PICO-I |
| `pico_comparator` | PICO-C |
| `pico_outcome` | PICO-O |
| `inclusion_criteria` | 纳入标准 |
| `exclusion_criteria` | 排除标准 |
| `id` | 项目主键编号 |
| `status` | 当前项目状态 |

#### `export_bundle.citations`

这里是一条条文献记录。

每条文献常见字段如下：

| 子字段 | 含义 | 白话解释 |
|---|---|---|
| `id` | 文献编号 | 这篇文献在系统里的内部编号 |
| `project_id` | 项目编号 | 它属于哪个综述项目 |
| `source` | 文献来源 | 来自哪个数据库 |
| `external_id` | 外部编号 | 比如 PMID，或者自定义编号 |
| `title` | 标题 | 文章题目 |
| `abstract` | 摘要 | 文章摘要 |
| `authors` | 作者 | 作者列表 |
| `publication_year` | 年份 | 文章发表年份 |
| `doi` | DOI | 学术文献唯一标识之一 |
| `is_deduplicated` | 是否重复 | `true` 表示这条被判定为重复 |
| `dedup_group` | 重复分组 | 未来用于标记重复组，当前多为空 |

#### `export_bundle.prisma`

这是系统综述里很重要的统计区。

| 子字段 | 含义 | 白话解释 |
|---|---|---|
| `project_id` | 项目编号 | 属于哪个综述项目 |
| `identified_count` | 初始识别数 | 最开始拿到多少篇文献 |
| `deduplicated_count` | 去重后数量 | 去重后剩多少篇 |
| `screened_count` | 已筛选数量 | 已经做过纳入/排除判断的数量 |
| `included_count` | 纳入数量 | 被保留下来的文献数 |
| `excluded_count` | 排除数量 | 被排除掉的文献数 |
| `full_text_assessed_count` | 全文评估数量 | 当前 MVP 还没真正启用 |

#### `export_bundle.audit_logs`

这是留痕记录，方便以后审计和回溯。

| 子字段 | 含义 |
|---|---|
| `id` | 日志编号 |
| `project_id` | 所属项目 |
| `action` | 动作类型 |
| `actor` | 执行人 |
| `summary` | 一句话摘要 |

常见动作类型包括：

| `action` | 中文解释 |
|---|---|
| `project.created` | 创建了综述项目 |
| `search_strategy.generated` | 生成了检索式 |
| `citations.imported` | 导入了文献 |
| `citations.deduplicated` | 完成了去重 |

## 4. Workflow 2 字段说明

## 4.1 输入字段

| 字段名 | 中文含义 | 你怎么理解 |
|---|---|---|
| `project_id` | 项目编号 | 你要操作哪个综述项目 |
| `citation_id` | 文献编号 | 你正在判断哪一篇文献 |
| `decision` | 筛选决策 | 这篇是纳入还是排除 |
| `reason` | 决策理由 | 为什么纳入或排除 |
| `actor` | 执行人 | 谁做了这次筛选 |

### 4.2 输入字段白话解释

#### `project_id`

项目编号。没有它，系统不知道你是在处理哪一个综述项目。

#### `citation_id`

文献编号。这个值不是外部 PMID，而是系统内部 id。

#### `decision`

当前只支持两种值：

| 值 | 含义 |
|---|---|
| `include` | 纳入 |
| `exclude` | 排除 |

#### `reason`

理由非常重要，因为真实科研工作需要留下决策依据。

示例：

`RCT，研究对象和结局均匹配综述问题。`

#### `actor`

执行人，可以写研究者姓名、账号名或角色名。

示例：

`novice-demo`

## 4.3 输出字段

| 字段名 | 中文含义 | 你怎么理解 |
|---|---|---|
| `decision_id` | 决策编号 | 这次筛选动作自己的流水号 |
| `prisma_summary` | PRISMA 汇总 | 提交本次筛选后的最新统计结果 |

## 5. 我们已经真实跑过的 3 个项目

## 5.1 项目总览

| 项目主题 | `project_id` | `imported_count` | `removed_count` | 当前状态 |
|---|---:|---:|---:|---|
| SGLT2 抑制剂与 2 型糖尿病患者心衰住院风险 | 3 | 4 | 1 | `exported` |
| PD-1 抑制剂联合化疗用于可切除胃癌新辅助治疗 | 4 | 4 | 1 | `exported` |
| 脓毒症患者早期限制性液体复苏策略 | 5 | 4 | 1 | `exported` |

说明：

1. 每个项目都导入了 4 条文献
2. 每个项目都成功识别出 1 条重复文献
3. 所有项目都已经完成“建项目 -> 生成检索式 -> 导入 -> 去重 -> 导出”闭环

## 5.2 项目一：SGLT2 与心衰住院

### 输入示例

| 字段 | 值 |
|---|---|
| `title` | SGLT2 抑制剂与 2 型糖尿病患者心衰住院风险文献综述 |
| `research_question` | 在 2 型糖尿病患者中，SGLT2 抑制剂是否降低心力衰竭住院风险？ |
| `pico_population` | 成人 2 型糖尿病患者 |
| `pico_intervention` | SGLT2 抑制剂 |
| `pico_comparator` | 安慰剂或标准治疗 |
| `pico_outcome` | 心力衰竭住院 |
| `inclusion_criteria` | 随机对照试验；成人；报告心衰住院或主要心血管结局。 |
| `exclusion_criteria` | 非临床研究；重复发表；无关键结局。 |

### 输出摘要

| 字段 | 值 |
|---|---|
| `project_id` | 3 |
| `imported_count` | 4 |
| `removed_count` | 1 |
| `search_query` | `("在 2 型糖尿病患者中，SGLT2 抑制剂是否降低心力衰竭住院风险？"[Title/Abstract]) AND ("成人 2 型糖尿病患者"[Title/Abstract]) AND ("SGLT2 抑制剂"[Title/Abstract]) AND ("心力衰竭住院"[Title/Abstract])` |

### 文献结果说明

有效保留文献 3 条，重复文献 1 条：

| 系统 `id` | `external_id` | 标题 | 是否重复 |
|---|---|---|---|
| 5 | 26378978 | Empagliflozin, Cardiovascular Outcomes, and Mortality in Type 2 Diabetes | `false` |
| 6 | 28605608 | Canagliflozin and Cardiovascular and Renal Events in Type 2 Diabetes | `false` |
| 7 | 30415602 | Dapagliflozin and Cardiovascular Outcomes in Type 2 Diabetes | `false` |
| 8 | 26378978-dup | Empagliflozin, Cardiovascular Outcomes, and Mortality in Type 2 Diabetes | `true` |

### 这说明了什么

1. 系统能把同一篇文章的重复条目识别出来
2. 系统内部的文献 `id` 和外部数据库编号 `external_id` 是两回事
3. 后续做筛选时，应该使用系统内部的 `citation_id`

## 5.3 项目二：胃癌免疫新辅助

### 输入主题

`PD-1 抑制剂联合化疗用于可切除胃癌新辅助治疗的文献综述`

### 输出摘要

| 字段 | 值 |
|---|---|
| `project_id` | 4 |
| `imported_count` | 4 |
| `removed_count` | 1 |
| `deduplicated_count` | 3 |

### 这说明了什么

1. 同一套 workflow 可以复用于完全不同病种
2. 系统本质上是“科研流程壳子 + 可替换的研究主题”
3. 只要你换题目和文献列表，就能复用这套 agent

## 5.4 项目三：脓毒症早期液体复苏

### 输入主题

`脓毒症患者早期限制性液体复苏策略文献综述`

### 输出摘要

| 字段 | 值 |
|---|---|
| `project_id` | 5 |
| `imported_count` | 4 |
| `removed_count` | 1 |
| `deduplicated_count` | 3 |

### 关键文献示例

| 系统 `id` | `external_id` | 标题 | 是否重复 |
|---|---|---|---|
| 13 | 36688507 | Early Restrictive or Liberal Fluid Management for Sepsis-Induced Hypotension | `false` |
| 14 | 24635773 | Protocolized Care for Early Septic Shock | `false` |
| 15 | 24635770 | Trial of Early, Goal-Directed Resuscitation for Septic Shock | `false` |
| 16 | 36688507-DUP | Early Restrictive or Liberal Fluid Management for Sepsis-Induced Hypotension | `true` |

## 6. Workflow 2 真实筛选演示

为了避免历史测试数据干扰，这里使用项目三 `project_id = 5` 做干净演示。

## 6.1 第一次筛选：纳入

### 输入

| 字段 | 值 |
|---|---|
| `project_id` | 5 |
| `citation_id` | 13 |
| `decision` | `include` |
| `reason` | 纳入演示：研究对象、干预和结局均匹配。 |
| `actor` | `novice-demo` |

### 输出

| 字段 | 值 |
|---|---|
| `decision_id` | 4 |
| `identified_count` | 4 |
| `deduplicated_count` | 3 |
| `screened_count` | 1 |
| `included_count` | 1 |
| `excluded_count` | 0 |

### 这说明了什么

系统记住了：

1. 这个项目一开始有 4 条记录
2. 去重后有 3 条有效记录
3. 你已经筛了 1 条
4. 目前纳入 1 条，排除 0 条

## 6.2 第二次筛选：排除

### 输入

| 字段 | 值 |
|---|---|
| `project_id` | 5 |
| `citation_id` | 14 |
| `decision` | `exclude` |
| `reason` | 排除演示：这里模拟结局指标与综述重点不完全一致。 |
| `actor` | `novice-demo` |

### 输出

| 字段 | 值 |
|---|---|
| `decision_id` | 5 |
| `identified_count` | 4 |
| `deduplicated_count` | 3 |
| `screened_count` | 2 |
| `included_count` | 1 |
| `excluded_count` | 1 |

### 这说明了什么

提交第二次筛选之后：

1. 已筛选数从 1 变成 2
2. 纳入仍是 1
3. 排除变成 1

这就是 PRISMA 统计自动更新的最小闭环。

## 7. 一个科研小白最需要先记住的字段

如果你现在不想记太多，只先记住下面这些：

| 字段 | 为什么重要 |
|---|---|
| `project_id` | 找到一个综述项目 |
| `citation_id` | 找到项目里的某一篇文献 |
| `search_query` | 看系统给出的检索草案 |
| `imported_count` | 看导入了多少篇 |
| `removed_count` | 看去掉了多少重复篇 |
| `decision` | 这篇是纳入还是排除 |
| `reason` | 为什么这么判断 |
| `prisma_summary` | 看整个综述筛选进度 |

## 8. 当前 MVP 已经能做什么

当前这套 MVP 已经可以稳定演示：

1. 建立一个综述项目
2. 生成基础检索式
3. 导入结构化文献清单
4. 自动去重
5. 导出项目包
6. 对单篇文献做纳入/排除判断
7. 自动更新 PRISMA 计数
8. 保留操作日志

## 9. 当前 MVP 还不能做什么

当前还没有完全打通以下能力：

1. 直接连接 PubMed、Embase、知网、万方实时检索
2. 自动解析 PDF 全文
3. 自动提取基线表、效应值、结局指标
4. 自动做 NOS / Cochrane 偏倚风险评价
5. 自动生成 PRISMA 流程图图片
6. 自动输出完整系统综述初稿

## 10. 一句话总结

这套文献综述智能体当前最适合这样理解：

`它不是一个只会聊天的 AI，而是一个能帮你把文献综述流程“搭项目、管文献、做筛选、记统计”的科研流程助手。`
