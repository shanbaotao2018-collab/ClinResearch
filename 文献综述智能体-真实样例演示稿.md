# 文献综述智能体真实样例演示稿

更新日期：2026-07-01

## 1. 演示目标

验证“文献综述智能体”MVP 是否已经具备以下最小闭环能力：

1. 创建综述项目
2. 自动生成检索式
3. 导入真实文献样例
4. 自动去重
5. 输出结构化项目包
6. 提交筛选决策并实时更新 PRISMA 计数

本次演示基于本地部署的：

- Dify 工作流宿主
- Literature Review Agent FastAPI 业务后端

## 2. 本次真实样例

### 2.1 综述主题

`SGLT2 抑制剂与 2 型糖尿病患者心衰住院风险文献综述`

### 2.2 研究问题

`在 2 型糖尿病患者中，SGLT2 抑制剂是否降低心力衰竭住院风险？`

### 2.3 PICO

- Population：成人 2 型糖尿病患者
- Intervention：SGLT2 抑制剂
- Comparator：安慰剂或标准治疗
- Outcome：心力衰竭住院

### 2.4 纳排标准

- 纳入：随机对照试验；成人；报告心衰住院或主要心血管结局
- 排除：非临床研究；重复发表；无关键结局

### 2.5 导入文献样例

本次导入 4 条真实/准真实文献记录，其中包含 1 条重复记录，用于验证去重能力：

1. EMPA-REG OUTCOME
2. CANVAS
3. DECLARE-TIMI 58
4. EMPA-REG OUTCOME 重复记录

## 3. 主工作流演示结果

工作流名称：`文献综述-项目初始化与检索准备`

执行结果：`成功`

### 3.1 主输出

- `project_id = 3`
- `imported_count = 4`
- `removed_count = 1`

### 3.2 自动生成的检索式

```text
("在 2 型糖尿病患者中，SGLT2 抑制剂是否降低心力衰竭住院风险？"[Title/Abstract]) AND ("成人 2 型糖尿病患者"[Title/Abstract]) AND ("SGLT2 抑制剂"[Title/Abstract]) AND ("心力衰竭住院"[Title/Abstract])
```

说明：

- 当前策略生成逻辑已闭环跑通
- 这版检索式更像“规则生成的基础检索草案”
- 后续可继续升级为更专业的 PubMed 检索表达式生成器

### 3.3 去重结果

- 共导入 4 条文献
- 去重移除 1 条
- 重复文献被正确标记为 `is_deduplicated = true`

### 3.4 导出结果内容

工作流最终返回了完整 `export_bundle`，包含：

- `project`：项目基本信息
- `citations`：当前项目下全部文献
- `prisma`：PRISMA 统计
- `audit_logs`：操作审计日志

这说明当前系统已经不是“只会聊天”，而是具备了基础业务状态管理能力。

## 4. 文献结果摘录

本次导出后的有效文献记录包括：

1. `Empagliflozin, Cardiovascular Outcomes, and Mortality in Type 2 Diabetes`
   - external_id: `26378978`
   - doi: `10.1056/NEJMoa1504720`

2. `Canagliflozin and Cardiovascular and Renal Events in Type 2 Diabetes`
   - external_id: `28605608`
   - doi: `10.1056/NEJMoa1611925`

3. `Dapagliflozin and Cardiovascular Outcomes in Type 2 Diabetes`
   - external_id: `30415602`
   - doi: `10.1056/NEJMoa1812389`

4. 重复记录
   - external_id: `26378978-dup`
   - `is_deduplicated = true`

## 5. 筛选工作流演示结果

工作流名称：`文献综述-筛选决策与PRISMA`

执行结果：`成功`

本次额外演示了 2 次人工筛选决策提交：

### 5.1 第 1 次：纳入

输入：

- `project_id = 3`
- `citation_id = 1`
- `decision = include`

输出结果：

- `decision_id = 1`
- PRISMA 更新为：
  - `identified_count = 4`
  - `deduplicated_count = 3`
  - `screened_count = 1`
  - `included_count = 1`
  - `excluded_count = 0`

### 5.2 第 2 次：排除

输入：

- `project_id = 3`
- `citation_id = 2`
- `decision = exclude`

输出结果：

- `decision_id = 2`
- PRISMA 更新为：
  - `identified_count = 4`
  - `deduplicated_count = 3`
  - `screened_count = 2`
  - `included_count = 1`
  - `excluded_count = 1`

## 6. 演示结论

### 6.1 已经验证通过的能力

当前这套 MVP 已经真实跑通以下闭环：

1. 课题初始化
2. 检索式生成
3. 手工文献导入
4. 去重
5. 项目包导出
6. 人工筛选决策提交
7. PRISMA 统计实时更新
8. 全流程留痕审计

### 6.2 这意味着什么

这说明“文献综述智能体”已经具备一个可演示、可扩展、可复用的最小业务闭环，不再只是一个问答机器人，而是：

- 有状态
- 有数据结构
- 有流程编排
- 有审计记录
- 可以继续接入外部专业能力

### 6.3 当前最适合作为对内演示的定位

建议当前定位为：

`医学科研文献综述智能体 MVP（项目初始化 + 文献管理 + 去重 + 筛选 + PRISMA）`

## 7. 当前不足

虽然闭环已跑通，但目前仍属于 MVP，主要差距有：

1. 检索式生成还不够“医学数据库专家级”
2. 当前文献导入仍以手工/结构化导入为主，尚未直接打通 PubMed / Embase / CNKI / 万方实时检索
3. 尚未实现基线表、效应值、结局指标的自动抽取
4. 尚未实现 NOS / Cochrane 偏倚风险自动评价
5. 尚未自动生成 PRISMA 流程图图片
6. 尚未形成完整“系统评价写作”终稿输出

## 8. 下一步建议

建议按“小步快跑”继续升级：

### 第一阶段

- 接 PubMed / Europe PMC 实时检索
- 优化检索式生成模板
- 增加标题/摘要初筛助手

### 第二阶段

- 增加全文信息抽取
- 增加基线特征与结局字段抽取
- 增加证据表导出

### 第三阶段

- 增加 NOS / Cochrane 风险偏倚评价
- 增加 PRISMA 流程图自动出图
- 增加综述初稿生成

## 9. 本次落地说明

为保证本地演示能真实跑通，本次做了两项最小修正：

1. 放开 Dify 到本地业务后端的 SSRF allowlist
2. 将 workflow 中依赖缺失插件的 `json_parse` 节点替换为 Dify 原生 `code` 节点

这两项修改不影响整体技术路线，反而让当前本地 MVP 更稳定可演示。

## 10. 一句话汇报版本

`文献综述智能体 MVP 已完成本地闭环验证，已真实跑通“项目初始化-检索式生成-文献导入-去重-筛选-PRISMA更新”全流程，具备继续向系统评价与科研辅助平台演进的基础。`
