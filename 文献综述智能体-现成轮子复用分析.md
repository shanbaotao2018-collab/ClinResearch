# 文献综述智能体：现成轮子复用分析

## 1. 目标定义

面向医疗科研场景，建设一个“文献综述智能体”，目标不是单纯做问答，而是尽量覆盖系统综述/Meta 分析/课题前期调研的关键工作流，满足以下核心要求：

1. 一站式整合中外医学数据库检索。
2. AI 自动生成标准检索式。
3. 批量解析文献，提取基线、效应指标、结局数据。
4. 支持 PRISMA 流程图。
5. 支持 NOS / Cochrane 偏倚风险评价。
6. 自动梳理领域研究脉络与争议点。
7. 尽可能减少人工读文、筛文、整理综述的时间。

结论先说：

- 这件事可以做，但不能指望“一个开源项目全包”。
- 最现实的路径不是从零做一套平台，而是把一批成熟开源组件拼成一个工作流型 agent。
- 如果追求最小开发量，应优先做“英文数据库优先 + 中文数据库导入补齐 + 人机协同审阅”的 MVP。

---

## 2. 这不是一个单点 agent，而是一个工作流 agent

“文献综述智能体”本质上不是单轮对话助手，而是一条多阶段流水线。拆开看，至少包含 8 个能力段：

1. 检索源接入
2. 检索式生成
3. 文献去重与题录筛选
4. 全文获取与 PDF 解析
5. 结构化信息抽取
6. PRISMA 流程统计与出图
7. 偏倚风险评价与可视化
8. 研究脉络、争议点、综述初稿生成

所以正确的选型方法不是找“最强 agent”，而是给每个环节找最成熟的轮子。

---

## 3. 按能力环节拆解：哪些轮子可以直接复用

## 3.1 检索源接入：国外医学数据库

这一层最成熟，基本不建议自研。

优先复用：

- `pubmed-mcp-server`
  - 适合接 PubMed、Europe PMC、PMC、MeSH。
  - 能力通常包括：文献检索、摘要拉取、PMID/PMCID/DOI 转换、相似文献、引用信息、MeSH 主题词辅助。
  - 价值：它已经把 PubMed 相关能力包装成 agent 可调用工具，适合直接挂到宿主平台里。

- PubMed API / E-utilities
  - 最基础、最稳定的英文医学检索底座。
  - 适合做标准检索、题录获取、元数据拉取。

- Europe PMC API
  - 相比 PubMed，通常在开放全文、预印本、补充信息覆盖上更友好。
  - 对全文可获得性和引用扩展分析有帮助。

- Crossref API
  - 用于 DOI 补全、元数据对齐、期刊信息补全。

- ClinicalTrials.gov API
  - 如果你们后续要做研究空白识别、综述补充临床试验登记，这个很有价值。

建议判断：

- 如果只做医疗文献综述，`PubMed + Europe PMC + Crossref` 基本够起一个英文 MVP。
- 如果要更像科研助手，再补 `ClinicalTrials.gov`。

## 3.2 检索源接入：中文医学数据库

这一层是整个产品最难的地方，不是算法难，而是数据接入难。

业务上必须考虑的中文源：

- CNKI
- 万方
- SinoMed / 中国生物医学文献数据库

现实判断：

- 这几个库普遍不适合简单按“公开 API + 开源接入”方式做。
- 完全依赖爬虫方案，不稳定，也不适合做正式产品。
- 真正可落地的路线通常是“授权接口接入”或者“人工/半自动导出题录后导入系统”。

最务实的做法：

1. 第一阶段先把中文数据库设计成“导入式数据源”。
2. 支持 RIS / NBIB / EndNote XML / BibTeX / CSV 导入。
3. 如果后续有正式授权，再把 CNKI / 万方 / SinoMed 做成直连源。

可复用桥接工具：

- `Zotero`
  - 不是医学专用平台，但非常适合作为题录汇聚中间层。
  - 价值在于：导入、去重、导出格式丰富，生态成熟。

结论：

- 中文数据库直连不是前期 MVP 的优先项。
- 中文数据库“导入 + 解析 + 去重 + 后续统一处理”是最小成本可落地路线。

## 3.3 AI 自动生成标准检索式

这一层不能完全靠大模型自由发挥，必须“LLM + 检索规则模板”结合。

可复用的轮子：

- `medical-research-skills`
  - 很适合做研究问题拆解、PICO 拆解、检索意图转化、综述写作提示。
  - 适合作为“检索式生成 agent”的提示与流程框架。

- MeSH 词表 / PubMed Query Builder 逻辑
  - 不能省。
  - 标准医学检索式必须把自由词、主题词、布尔逻辑、字段限定结合起来。

最合理的实现方式：

1. 用户输入研究问题。
2. agent 把问题拆成 PICO 或主题块。
3. 结合 MeSH 词表补充同义词、上位词、缩写。
4. 自动生成：
   - PubMed 检索式
   - Embase 风格检索式（如后续支持）
   - 中文数据库关键词串
5. 允许人工编辑后执行检索。

结论：

- 这层可以用 `medical-research-skills` 做“脑子”。
- 但检索式规则模板必须自己沉淀，不能只靠 prompt。

## 3.4 文献去重与题录筛选

这一层开源轮子很多，而且成熟度高，不建议自研。

优先复用：

- `ASReview`
  - 系统综述领域非常成熟的主动学习筛文工具。
  - 适合标题/摘要阶段的半自动纳排筛选。
  - 特点是越筛越准，能明显减少人工工作量。

- `AiReview`
  - 一个更偏平台化的开源项目，通常包含前后端、任务队列、LLM 辅助 screening 能力。
  - 比较适合你们如果要做成网页型产品去复用其工作流设计。

- `revtools`
  - R 生态里很成熟。
  - 擅长去重、题录清洗、标题摘要筛选、主题聚类。

建议判断：

- 如果要“最小代码量”做 Web MVP，优先研究 `AiReview` 是否能直接当 screening 子模块。
- 如果要“算法成熟度”与学术圈认可度，`ASReview + revtools` 更稳。

## 3.5 全文获取与 PDF 解析

这一层也不建议自研，尤其不要自己重做学术 PDF 解析。

优先复用：

- `GROBID`
  - 学术 PDF 结构化解析的老牌方案。
  - 对英文论文的标题、摘要、章节、参考文献、表格元信息处理较成熟。

- `MinerU`
  - 对中文文档、复杂版式、扫描文档支持通常更实用。
  - 很适合做中文论文 PDF / 扫描版资料解析。

- `PP-StructureV2` / `PaddleOCR`
  - 用于 OCR、版面分析、表格区域识别。
  - 对中文 PDF 和图片型文档特别有帮助。

推荐组合：

- 英文论文：`GROBID` 优先。
- 中文 PDF / 扫描件：`MinerU` 或 `PaddleOCR` 补充。

结论：

- 不要自己做 PDF parser。
- 你们只需要做“统一抽象层”，把不同 parser 的输出归一成统一 JSON。

## 3.6 基线、效应指标、结局数据提取

这是最关键、也最难完全自动化的一层。

现实判断：

- 目前几乎没有一个开源项目能把医学文献中的基线表、效应量、结局指标、亚组分析结果稳定地一键抽干净。
- 这一层通常要靠“解析器 + 表格抽取 + LLM 结构化 + 人工复核”组合。

可复用的部分：

- `GROBID` / `MinerU` / `PaddleOCR`
  - 负责把全文和表格先解析出来。

- 通用表格抽取组件
  - Camelot / Tabula 这一类对规则型 PDF 表格有帮助，但医学论文复杂表格场景经常不稳。

- 大模型结构化抽取
  - 非开源“项目轮子”，但这是目前最实用的一层。
  - 要求模型按严格 JSON Schema 输出：
    - 研究设计
    - 纳入样本量
    - 干预/暴露
    - 对照
    - 基线特征
    - 主要结局
    - 次要结局
    - 效应指标
    - 置信区间
    - P 值
    - 随访时间

这一层真正要自建的不是模型，而是“抽取 schema”：

- RCT 抽取 schema
- 队列研究抽取 schema
- 病例对照研究抽取 schema
- 诊断试验抽取 schema
- 预后研究抽取 schema

结论：

- 这里没有现成全自动轮子可以直接拿来交付。
- 但可以大量复用解析基础设施，把真正开发量压缩到“schema + 校验 + 人审界面”。

## 3.7 PRISMA 流程图

这层成熟度很高，完全不建议自研绘图。

可复用：

- `PRISMA 2020` 官方模板 / Shiny 工具
  - 本质上你只要把每一步筛选数量统计出来，就能自动出图。

你们要做的只有两件事：

1. 定义筛选阶段状态机。
2. 自动累计每阶段数量。

例如：

- 检索得到多少篇
- 去重后多少篇
- 标题摘要初筛排除多少篇
- 全文评估多少篇
- 最终纳入多少篇

结论：

- 这一层几乎零算法难度。
- 重点不是图，而是前面工作流数据要记账。

## 3.8 NOS / Cochrane 偏倚风险评价

这一层不适合做“全自动判分”，更适合做“证据提取 + AI 预判 + 人工确认”。

可复用：

- `robvis`
  - 学术界很常用。
  - 适合生成 ROB2、ROBINS-I、QUADAS-2、QUIPS 等偏倚风险可视化图。

推荐实现：

1. agent 先抽取与偏倚判断相关的证据句段。
2. 按研究类型进入不同评价模板。
3. AI 给出初步判断和理由。
4. 由人工确认后生成最终评价结果。
5. 调用 `robvis` 生成标准化图表。

结论：

- `robvis` 负责“出图”。
- 判断逻辑与证据链整理需要你们自己做产品层。

## 3.9 自动梳理研究脉络与争议点

这一层如果纯靠 LLM，容易变成“会说但不可靠”。

更稳的做法是“文献计量分析 + LLM 解释层”。

可复用：

- `bibliometrix / Biblioshiny`
  - 文献计量分析成熟工具。
  - 可以做关键词共现、主题演化、作者合作网络、国家机构分布、热点趋势等。

- `revtools`
  - 也可辅助做主题聚类和语义聚类。

推荐实现：

1. 先做题录层面的聚类与计量。
2. 再让 LLM 基于聚类结果、时间演化、结局差异总结研究脉络。
3. 争议点由以下模式自动识别：
   - 结论方向相反
   - 人群差异导致结果不一致
   - 干预剂量/时长不同
   - 随访时间不同
   - 研究设计质量差异

结论：

- 脉络与争议点总结可以做得很像“高级 agent”。
- 但它的底座应该是 bibliometric 结果，不应只靠聊天模型。

---

## 4. 各开源轮子与需求匹配度总结

| 能力模块 | 可复用项目/组件 | 完成度 | 是否建议直接复用 | 备注 |
| --- | --- | --- | --- | --- |
| 国外文献检索 | `pubmed-mcp-server`、PubMed API、Europe PMC API | 高 | 是 | 最成熟 |
| 中文文献接入 | Zotero 导入桥接、授权接口 | 中 | 是，但以导入为主 | 直连最难 |
| 检索式生成 | `medical-research-skills` + MeSH 规则 | 中高 | 部分复用 | 提示词可复用，规则模板要自建 |
| 去重与筛选 | `ASReview`、`AiReview`、`revtools` | 高 | 是 | 最不该自研的一层 |
| PDF 解析 | `GROBID`、`MinerU`、`PaddleOCR` | 高 | 是 | 英文/中文各有强项 |
| 数据抽取 | LLM + parser + schema | 中 | 部分复用 | 真正产品壁垒在这里 |
| PRISMA | 官方模板 / Shiny | 高 | 是 | 不要自己画 |
| 偏倚风险图 | `robvis` | 高 | 是 | 判断逻辑仍需人机协同 |
| 文献计量/脉络 | `bibliometrix`、`Biblioshiny` | 高 | 是 | 很适合做高阶能力 |

---

## 5. 哪些坚决不要自己做

以下模块直接复用成熟开源组件，性价比最高：

1. PubMed / PMC / Europe PMC 检索接入
2. 标题摘要筛选引擎
3. 去重引擎
4. 学术 PDF 解析器
5. PRISMA 流程图生成
6. 偏倚风险可视化图生成
7. 文献计量分析引擎

原因很简单：

- 这些问题已经有成熟解法。
- 自研开发量大，但不会形成你们的差异化壁垒。
- 一旦自研，准确率、边界案例、学术认可度、长期维护成本都会变差。

---

## 6. 哪些必须自己做

真正需要你们自己沉淀的，是产品层与领域层，而不是底层工具本身。

必须自建的部分：

1. 中文数据库接入策略
2. 医学研究问题到检索式的规则模板
3. 各研究类型的数据抽取 schema
4. 抽取结果的校验规则
5. 偏倚风险评价的人机协同界面
6. 文献综述工作流编排
7. 最终“综述结论、研究脉络、争议点”的整合输出逻辑

这里才是真正贴近业务要求、也能沉淀成产品能力的部分。

---

## 7. 如果按“最小开发量”做 MVP，推荐怎么拼

## 7.1 最小可落地英文 MVP

建议组合：

- 宿主层：现成 B/S agent 宿主
- 检索层：`pubmed-mcp-server`
- 筛选层：`ASReview` 或 `AiReview`
- 全文解析层：`GROBID`
- 偏倚可视化：`robvis`
- 脉络分析：`bibliometrix`
- 智能编排层：你们自己的 agent orchestration
- 研究提示与写作层：`medical-research-skills`

这个版本能解决什么问题：

- 自动生成 PubMed 检索式
- 自动批量抓题录
- 自动去重与标题摘要初筛
- 批量解析摘要/全文
- 辅助提取研究设计、样本量、主要结局
- 自动生成 PRISMA
- 自动生成偏倚风险图
- 自动总结研究热点、争议点、综述初稿

## 7.2 更适合国内场景的二阶段版本

在 MVP 跑通后补：

- 中文数据库导入能力
- 中文 PDF 解析能力
- 中文医学术语归一化
- 中英题录去重与对齐
- 中文综述草稿能力

推荐补充组件：

- `Zotero`
- `MinerU`
- `PaddleOCR`

---

## 8. 对你们当前需求的实际建议

如果目标是“尽快做出一个能演示、能闭环、开发量最小的文献综述智能体”，建议路线不是“一步到位全功能”，而是分三层推进。

### 第一层：先做能跑通的闭环

范围建议：

- PubMed / Europe PMC 检索
- 自动生成检索式
- 题录导入与去重
- 标题摘要筛选
- PRISMA 自动生成
- 初步脉络总结

这已经能形成一个像样的“文献综述智能体 MVP”。

### 第二层：补全文解析与结构化抽取

范围建议：

- PDF 批量解析
- 研究设计识别
- 基线/结局/效应量抽取
- 人工复核台

这一层决定产品是否真正有科研生产力。

### 第三层：补偏倚评价与系统综述级输出

范围建议：

- NOS / ROB2 / ROBINS-I 模板化评价
- `robvis` 可视化
- 自动生成系统综述草稿
- 方法学章节辅助撰写

这一层完成后，才更接近“系统综述 copilot”而不只是“文献搜索助手”。

---

## 9. 最终建议：优先复用的开源项目清单

如果只保留最值得优先研究的 8 个轮子，我建议是：

1. `medical-research-skills`
   - 负责研究拆解、检索式生成、综述写作提示。

2. `pubmed-mcp-server`
   - 负责国外医学数据库检索接入。

3. `ASReview`
   - 负责主动学习式筛文。

4. `AiReview`
   - 负责更产品化的筛文工作流参考。

5. `GROBID`
   - 负责英文论文 PDF 解析。

6. `MinerU`
   - 负责中文或复杂版式 PDF 解析。

7. `robvis`
   - 负责偏倚风险图表输出。

8. `bibliometrix / Biblioshiny`
   - 负责研究脉络、热点、趋势、争议分析底座。

---

## 10. 一句话结论

“文献综述智能体”最合理的建设方式，不是找一个现成开源 agent 直接套，而是：

以 `medical-research-skills` 作为研究任务脑子，以 `pubmed-mcp-server` 作为检索工具层，以 `ASReview/AiReview` 作为筛选层，以 `GROBID/MinerU` 作为解析层，以 `robvis` 和 `bibliometrix` 作为专业输出层，再由你们自己做工作流编排、抽取 schema 和人机协同界面。

这条路线开发量最小，也最符合“99% 的问题别人已经解决过了，不重复造轮子”的原则。
