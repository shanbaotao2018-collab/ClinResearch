interface:
  display_name: "PDF Extract"
  short_description: "Extract PDF text and page images (including tables)."
  default_prompt: "Extract text and render each PDF page to images; save Markdown with page headings and image links."
